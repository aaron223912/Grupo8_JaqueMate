<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head', { title : 'registro' }) %>
</head>

<body class="registro">
    <header class="registro__header">
        <div class="registro__header__logo">
            <a href="/"><img src="/images/jaqueMate.png" alt="" /></a>
        </div>
    </header>
    <main class="registro__main">
        <h1>Registro</h1>
        <p>Completá los siguientes datos</p>

        <form action="/users/storeUsers" class="registro__main__formulario" method="post">
            <div>
                <label for="nombre">Nombre</label>
                <input type="text" name="nombre" id="nombre" value="<%= locals.old && old.nombre?old.nombre:null %>" />
                <small class="nombreError">
                    <%= locals.errors && errors.nombre?errors.nombre.msg:null %>
                </small>
            </div>
            <div>
                <label for="apellido">Apellido</label>
                <input type="text" name="apellido" id="apellido"
                    value="<%= locals.old && old.apellido?old.apellido:null %>" />
                <small class="apellidoError">
                    <%= locals.errors && errors.apellido?errors.apellido.msg:null %>
                </small>
            </div>

            <div>
                <label for="email">Email</label>
                <input type="email" name="email" id="email" value="<%= locals.old && old.email?old.email:null %>" />
                <small class="emailError">
                    <%= locals.errors && errors.email?errors.email.msg:null %>
                </small>
            </div>

            <div class="pass-conteiner">
                <label for="password">Contraseña</label>
                <input type="password" name="password" id="password" />
                <span id="btn-show-pass"><i class="fa-solid fa-eye-slash" ></i></span>
                <small class="passwordError">
                    <%= locals.errors && errors.password ? errors.password.msg : null %>
                </small>
            </div>

            <div>
                <label for="password2">Confirma Contraseña</label>
                <input type="password" name="password2" id="password2" />
                <small class="passwordError2">
                    <%= locals.errors && errors.password2 ? errors.password2.msg : null %>
                </small>
            </div>

            <label for="term">Terminos y Condiciones</label>
            <input type="checkbox" name="term" id="term" />
            <small class="termError">
                <%= locals.errors && errors.term?errors.term.msg:null %>
            </small>

            <div class="registro__main__formulario__confirmar">
                <button type="submit" class="btn-enviar">Crear cuenta</button>
                <p>¿Ya tenés cuenta? <a href="/users/login">inicia sesión</a></p>
                <small class="error-form"></small>
            </div>
        </form>
    </main>
    <script src="/javascripts/userRegister.js"></script>
</body>

</html>